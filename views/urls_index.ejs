
<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<header>
    <% include ./_partials/_header %>
</header>
<body> 
<ul>
    <% for(each in url) { %>
    <% if(url[each].userId === user_id) { %>  
      <li>
        
        <%= `${each}` %> :
        <a href=<%= `/u/${each}/` %>> <%= url[each].url %></a>
        <a href=<%= `/urls/${each}/` %>>Edit</a>
        <form method='POST' action=<%= `/urls/${each}/delete`%>>
        <button type='submit' name='delete'>Delete</button>
      </form>
      </li>
    <% }; %>  
  <% }; %>
  <a href="/urls/new">Add a new URL</a>
</ul>
</body>
</html>
